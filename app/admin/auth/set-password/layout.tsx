import { Metadata } from "next";
import Page from "./page";

export const metadata: Metadata = {
  title: "Set Password - Flagway League",
};

export default function PageLayout() {
  return <Page />;
}
